# Provider recovery display boundary — SH-005 / PX-16

The actual generic LoggingInBody create/link failure handlers forward closed,
translatable connection/retry messages to existing profile/link views instead of
an arbitrary provider `error`. Such payloads may contain network targets, user
identities or markup. Handler names, route conditions, loader parameters apart
from errorString, existing spinner/button changes and activity events remain.
No credential or provider request/response semantics change; this does not enable
or newly support Oculus/Steam on any platform.

The complete two original handlers execute on real Qt JS with five input states
(four seeded provider values, then no ambient error binding), popup/no-popup,
create mode and three link modes. Loader, visual and activity objects are explicit
test boundaries, not native UI or whole-component evidence. Qt's actual qsTr is
used. Baseline prior source must fail. Existing original block extractor from
test_login_dialog_domain_receiver.py is a pinned source prerequisite.

Remaining: account request identity/foreground/deadline, native provider execution,
existing Steam failed-link recovery, general rich-text labels, other signal
consumers, screenshots/native export and full artifact/node privacy acceptance.
Choose the matching Main or Apple variant and retain previous outcome/diagnostic
contracts; no platform-owned source is changed.
