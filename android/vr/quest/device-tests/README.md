# Quest universal device adapter

The Quest adapter uses the shared Android ADB transport while retaining the
legacy Quest package identity and Quest-specific headset detection. It exposes
portable launch/idle tests and an XR-focus module.
