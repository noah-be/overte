# Shared Android VR code

Place code here only when it is used by more than one Android VR target. Pico-
and Quest-specific implementation remains in the adjacent target directories.

The directory is intentionally empty apart from this ownership rule until a
real shared VR component is identified; target code must not be generalized
solely to populate the directory.
