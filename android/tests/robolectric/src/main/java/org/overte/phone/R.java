package org.overte.phone;

/** Minimal generated-resource boundary; production Activity code remains unchanged. */
public final class R {
    private R() {
    }

    public static final class layout {
        // The launcher does not inspect this layout. Map its generated symbol
        // to a real framework layout so setContentView still inflates instead
        // of being silently defaulted by Robolectric.
        public static final int activity_startup = android.R.layout.simple_list_item_1;

        private layout() {
        }
    }
}
