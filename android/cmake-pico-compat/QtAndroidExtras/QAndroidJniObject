#pragma once

#include <QString>

// Pico builds do not need the Samsung-phone-only audio restart workaround
// which is the sole AndroidExtras use in the selected native target graph.
class QAndroidJniObject {
public:
    template<typename T>
    static QAndroidJniObject getStaticObjectField(const char*, const char*) {
        return {};
    }

    QString toString() const {
        return {};
    }
};
